# Notion integration token. Create at notion.so/my-integrations and share
# the parent page (or each database) with the integration.
NOTION_TOKEN=ntn_xxx

# Parent page housing the Maxam databases. Used by setup scripts only —
# the runtime fetchers read database IDs from scripts/output/notion-ids.json.
NOTION_PARENT_PAGE_ID=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
